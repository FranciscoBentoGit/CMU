<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".ui.home.activities.TransactionSearchActivity">>

    <item
        android:id="@+id/addUserACL"
        android:icon="@drawable/add"
        app:showAsAction="ifRoom"
        android:onClick="add_user_fragment"
        android:title="Add user" />

    <item
        android:id="@+id/share_private_link"
        app:showAsAction="never"
        android:onClick="chat_link_fragment"
        android:title="Share via link" />

    <item
        android:id="@+id/leave_chatroom"
        android:title="Leave Chatroom"
        app:showAsAction="never"
        android:onClick="leaveChat"/>

</menu>